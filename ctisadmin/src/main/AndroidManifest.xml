<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ctisadmin">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestReportActivity"></activity>
        <activity android:name=".UpdateResultActivity" />
        <activity android:name=".ResultListActivity" />
        <activity android:name=".ListPatientActivity" />
        <activity android:name=".ListTesterActivity" />
        <activity android:name=".RecordNewTestActivity" />
        <activity android:name=".MenuTesterActivity" />
        <activity android:name=".ManageTestKitStockActivity" />
        <activity android:name=".RecordTesterActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".RegisterTestCentreActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ListTestKitActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>